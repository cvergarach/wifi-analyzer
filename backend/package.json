{
  "name": "wifi-analyzer-backend",
  "version": "1.0.0",
  "description": "Backend API para WiFi Analyzer",
  "main": "dist/server.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node dist/server.js",
    "build": "esbuild src/server.js --bundle --platform=node --outfile=dist/server.js --external:pg-native"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "axios": "^1.6.2",
    "pg": "^8.11.3",
    "@google/generative-ai": "^0.1.3",
    "helmet": "^7.1.0",
    "compression": "^1.7.4",
    "express-rate-limit": "^7.1.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "esbuild": "^0.19.9"
  }
}
